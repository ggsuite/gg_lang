name: gg_lang
description: Shared language catalog and project-type detection for the gg toolchain.
version: 0.0.1
repository: https://github.com/ggsuite/gg_lang.git

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
